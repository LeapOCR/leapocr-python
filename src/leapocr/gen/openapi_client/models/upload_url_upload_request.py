"""
OCR API

OCR API for processing PDF documents with Gemini LLM integration

The version of the OpenAPI document: 1.0
Contact: support@clarivo.com
Generated by OpenAPI Generator (https://openapi-generator.tech)

Do not edit the class manually.
"""  # noqa: E501

from __future__ import annotations

import json
import pprint
import re  # noqa: F401
from typing import Any

from pydantic import BaseModel, Field, StrictStr, constr, validator


class UploadURLUploadRequest(BaseModel):
    """
    UploadURLUploadRequest
    """

    file_name: constr(strict=True, max_length=255, min_length=1) | None = None
    format: StrictStr | None = Field(
        default=None,
        description="Option 2: Direct specification (will create template on-the-fly)",
    )
    instructions: constr(strict=True, max_length=100) | None = None
    var_schema: dict[str, Any] | None = Field(default=None, alias="schema")
    template_id: StrictStr | None = Field(
        default=None, description="Option 1: Use existing template"
    )
    tier: StrictStr | None = None
    url: constr(strict=True, max_length=2000) = Field(...)
    __properties = [
        "file_name",
        "format",
        "instructions",
        "schema",
        "template_id",
        "tier",
        "url",
    ]

    @validator("format")
    def format_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in ("markdown", "structured", "per_page_structured"):
            raise ValueError(
                "must be one of enum values ('markdown', 'structured', 'per_page_structured')"
            )
        return value

    @validator("tier")
    def tier_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in ("swift", "core", "intelli"):
            raise ValueError("must be one of enum values ('swift', 'core', 'intelli')")
        return value

    class Config:
        """Pydantic configuration"""

        allow_population_by_field_name = True
        validate_assignment = True

    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.dict(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> UploadURLUploadRequest:
        """Create an instance of UploadURLUploadRequest from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self):
        """Returns the dictionary representation of the model using alias"""
        _dict = self.dict(by_alias=True, exclude={}, exclude_none=True)
        return _dict

    @classmethod
    def from_dict(cls, obj: dict) -> UploadURLUploadRequest:
        """Create an instance of UploadURLUploadRequest from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return UploadURLUploadRequest.parse_obj(obj)

        # raise errors for additional fields in the input
        for _key in obj.keys():
            if _key not in cls.__properties:
                raise ValueError(
                    "Error due to additional fields (not defined in UploadURLUploadRequest) in the input: "
                    + obj
                )

        _obj = UploadURLUploadRequest.parse_obj(
            {
                "file_name": obj.get("file_name"),
                "format": obj.get("format"),
                "instructions": obj.get("instructions"),
                "var_schema": obj.get("schema"),
                "template_id": obj.get("template_id"),
                "tier": obj.get("tier"),
                "url": obj.get("url"),
            }
        )
        return _obj
